{
    "build": {
        "env": {
            "APP_ENV": "production",
            "APP_DEBUG": "false"
        },
        "buildCommand": "composer install --no-dev && php artisan config:clear && php artisan key:generate && php artisan migrate --force",
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
    }
}
